// #include<stdio.h>
// int main(){
//     printf("ssup");
//     return 0;
// }
#include<stdio.h>
int main(){
    int weekendnos;
    scanf("%d\n",&weekendnos);
    int movienos[weekendnos];
    int movienossize=(sizeof(movienos)/sizeof(int));
    int j;
    for(int i=1;i<=weekendnos;i++){
        scanf("%d\n",&movienos[i]);
        int movielen[movienossize];
        int movierate[movienossize];
        for(j=1;j<=movienossize;j++){
            scanf("%d",&movielen[j]);
        }
        printf("\n");
        for(j=1;j<=movienossize;j++){
            scanf("%d",&movierate[j]);
        }
        int movielr[movienossize];
        for(j=1;j<=movienossize;j++){
            movielr[j]=(movielen[j]*movierate[j]);
        }
        for(j=)
        
    
        printf("Weekend nos is : %d\n",weekendnos);
        // printf("Movie nos is : %d\n",movienos[]);
        // printf("Movie len is : %d\n",movielen[]);
        // printf("Movie rate is : %d\n"movie rate[]);
    }

    return 0;
}
